name: Deploy Web to GitHub Pages

on:
  workflow_dispatch:
  repository_dispatch:
    types: [trigger-web]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy-web:
    uses: Giuig/ninja_material/.github/workflows/web-deploy.yml@main
    with:
      app-name: tvninja
